import React from "react";
import "./Title.css";

const Title = () => (
  <div className="game-title-div">
    <h1 className="game-title">React Concentration</h1>
    <h3 className="game-subtitle">Click on an image to earn points, but don't click an image more than once!</h3>
  </div>
);

export default Title;